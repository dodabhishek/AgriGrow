{
  "name": "newproject",
  "version": "1.0.0",
  "main": "./AgriGrow_backend/src/index.js",
  "scripts": {
    "build": "npm install && cd AgriGrow_backend && npm install --force && cd ../AgriGrow_frontend && npm install --force && npm run build",
    "start": "node ./AgriGrow_backend/src/index.js",
    "postinstall": "npm install --prefix AgriGrow_backend && npm install --prefix AgriGrow_frontend"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
